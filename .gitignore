# Local .terraform directories (contains downloaded providers and modules)
**/.terraform/

# Terraform state files (contains sensitive infra data and shouldn't be in Git)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log
